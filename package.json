{
  "name": "above-presentation",
  "version": "1.0.0",
  "description": "Above Runtime ITDR Technical Presentation",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:lint && npm run test:build && npm run test:e2e",
    "test:lint": "stylelint 'src/**/*.css' && echo '✅ CSS validation passed'",
    "test:build": "npm run build && node scripts/validate-build.js",
    "test:e2e": "playwright test",
    "test:unit": "echo 'Unit tests would go here'",
    "test:visual": "playwright test --grep 'visual'",
    "test:perf": "echo 'Performance tests would go here'",
    "lint": "stylelint 'src/**/*.css' --fix",
    "health-check": "node scripts/health-check.js",
    "deploy": "npm run build && echo 'Deploy via GitHub Actions'",
    "rollback": "echo 'Emergency rollback - revert last commit'",
    "prepare": "node scripts/setup-hooks.js"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0",
    "vite": "^5.0.8"
  },
  "type": "module"
}

